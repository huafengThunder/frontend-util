- babel-loader@9对于node的版本有要求的(babel-loader9.x	webpack5.x	npm^7.12.0	>= node14.15.0)
- 目前部署方案
  - 直接本地打包更新线上dist目录（提交时使用git commit --amend --no-edit合并上次提交信息）
- task
  - 抽取移动端、pc公共代码 
  - 建立后台模块
    - 登录模块
  - 自定义工具
    - base64转化
    - 命名转化，参考已有导航链接
- bug
  - module.less依旧会有影响,对于相同文件名字
- 优化
  - eslint虽然引入进来但是没有生效
  - card组件没有换行，太长了，容易引起压缩
  - 虽然是的两套代码，使用赖加载，但是还一些地方库还没有使用赖加载。还是会被一起打包进来
    - 使用webpack多页面打包。UA判断加载不同的出口页面（其实功能还没那么大的时候，赖加载其实暂时也是够了）
- 工作场景记录
  - 当遇到一个401就取消后续的401,基于axios库
- 每周博客选取
  - 关于sort(-)  ---1、使用坑点
  - JSON.parse ---1、使用坑点;2、对于对象无序,如何自己实现
  - 关于Crypto对象介绍
  - 关于跨域的理解，nginx反向代理的配置如何解决qiankunjs的子应用跨域问题。origin/referer/host三者区别
  - webpack的css modules方案。
    - 采用class name混淆的方式。所以使用标签名样式会影响全局
